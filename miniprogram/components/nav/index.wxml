<view class="nav-container">
	<image bind:tap="onPre" class="nav-icon" src="{{first? disLeftSrc:leftSrc}}" />
	<text class="nav-title">{{title}}</text>
	<image bind:tap="onNext" class="nav-icon" src="{{latest?disRightSrc:rightSrc}}" />
</view>